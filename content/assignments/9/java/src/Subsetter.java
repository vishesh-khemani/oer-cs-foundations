import java.util.ArrayList;
import java.util.List;
import java.util.Set;

/** Class to compute all possible unique subsets of items of type T. */
public class Subsetter<T extends Comparable<T>> {

    /**
     * Returns all possible unique subsets of items.
     * The items in a subset are ordered by their natural ordering.
     */
    public Set<List<T>> generateAllSubsets(ArrayList<T> items) {
        // TODO: implement this using the binary number strategy.
        throw new UnsupportedOperationException();
    }
}
